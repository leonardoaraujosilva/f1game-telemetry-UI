import { F1Parser } from '../F1Parser';
import { PacketHeader } from './types';
export declare class PacketHeaderParser extends F1Parser<PacketHeader> {
    constructor(packetFormat: number, bigintEnabled: boolean);
}
