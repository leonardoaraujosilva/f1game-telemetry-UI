/// <reference types="node" />
import { Parser } from 'binary-parser';
export declare class F1Parser<T> extends Parser {
    /**
     *
     * @param {Buffer} buffer
     */
    fromBuffer(buffer: Buffer): T;
}
