"use strict";

function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
var _index = require("./index");
var _nReadlines = _interopRequireDefault(require("n-readlines"));
var fs = _interopRequireWildcard(require("fs"));
function _getRequireWildcardCache(nodeInterop) { if (typeof WeakMap !== "function") return null; var cacheBabelInterop = new WeakMap(); var cacheNodeInterop = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(nodeInterop) { return nodeInterop ? cacheNodeInterop : cacheBabelInterop; })(nodeInterop); }
function _interopRequireWildcard(obj, nodeInterop) { if (!nodeInterop && obj && obj.__esModule) { return obj; } if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") { return { "default": obj }; } var cache = _getRequireWildcardCache(nodeInterop); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj["default"] = obj; if (cache) { cache.set(obj, newObj); } return newObj; }
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }
var normalize = function normalize(v) {
  return JSON.parse(JSON.stringify(v, function (key, value) {
    return typeof value === 'bigint' ? value.toString() : value;
  }));
};
var parseMessage = function parseMessage(data) {
  var _parsed$packetData;
  var parsed = _index.F1TelemetryClient.parseBufferMessage(Buffer.from(data), true);
  return normalize(parsed === null || parsed === void 0 ? void 0 : (_parsed$packetData = parsed.packetData) === null || _parsed$packetData === void 0 ? void 0 : _parsed$packetData.data);
};
describe('F1TelemetryClient', function () {
  describe('constructor', function () {
    describe('default settings', function () {
      describe('when no parameters are passed', function () {
        var f1TelemetryClient;
        beforeAll(function () {
          f1TelemetryClient = new _index.F1TelemetryClient();
        });
        it('should set default port, forwardAddresses and bigintEnabled to default values', function () {
          expect(f1TelemetryClient.port).toBe(_index.DEFAULT_PORT);
          expect(f1TelemetryClient.forwardAddresses).toBe(_index.FORWARD_ADDRESSES);
          expect(f1TelemetryClient.bigintEnabled).toBe(_index.BIGINT_ENABLED);
        });
        it('should set up client as udp4 client', function () {
          expect(f1TelemetryClient.socket).toBeDefined();
          // eslint-disable-next-line  @typescript-eslint/no-explicit-any
          expect(f1TelemetryClient.socket.type).toBe('udp4');
        });
      });
    });
    describe('port attribute', function () {
      describe('when a custom port is passed through parameters', function () {
        var f1TelemetryClient;
        beforeAll(function () {
          f1TelemetryClient = new _index.F1TelemetryClient({
            port: 20778
          });
        });
        it('should set custom port', function () {
          expect(f1TelemetryClient.port).toBe(20778);
        });
        it('should set forwardAddresses, forward port and bigintEnabled to default values', function () {
          expect(f1TelemetryClient.forwardAddresses).toBe(_index.FORWARD_ADDRESSES);
          expect(f1TelemetryClient.bigintEnabled).toBe(_index.BIGINT_ENABLED);
        });
        it('should set up client as udp4 client', function () {
          expect(f1TelemetryClient.socket).toBeDefined();
          // eslint-disable-next-line  @typescript-eslint/no-explicit-any
          expect(f1TelemetryClient.socket.type).toBe('udp4');
        });
      });
    });
    describe('parser enabled attribute', function () {
      describe('when parser enabled is passed through parameters', function () {
        var f1TelemetryClient;
        beforeAll(function () {
          f1TelemetryClient = new _index.F1TelemetryClient({
            forwardAddresses: [{
              port: 4477
            }]
          });
        });
        it('should set parser enabled', function () {
          expect(f1TelemetryClient.forwardAddresses).toStrictEqual([{
            port: 4477
          }]);
        });
        it('should set port, forward port and bigintEnabled to default values', function () {
          expect(f1TelemetryClient.port).toBe(_index.DEFAULT_PORT);
          expect(f1TelemetryClient.bigintEnabled).toBe(_index.BIGINT_ENABLED);
        });
        it('should set up client as udp4 client', function () {
          expect(f1TelemetryClient.socket).toBeDefined();
          // eslint-disable-next-line  @typescript-eslint/no-explicit-any
          expect(f1TelemetryClient.socket.type).toBe('udp4');
        });
      });
    });
    describe('bigintEnabled attribute', function () {
      describe('when bigint enabled is passed through parameters', function () {
        var f1TelemetryClient;
        beforeAll(function () {
          f1TelemetryClient = new _index.F1TelemetryClient({
            bigintEnabled: false
          });
        });
        it('should set bigint enabled', function () {
          expect(f1TelemetryClient.bigintEnabled).toBe(false);
        });
        it('should set forwardAddresses, forward port and port to default values', function () {
          expect(f1TelemetryClient.forwardAddresses).toBe(_index.FORWARD_ADDRESSES);
          expect(f1TelemetryClient.port).toBe(_index.DEFAULT_PORT);
        });
        it('should set up client as udp4 client', function () {
          expect(f1TelemetryClient.socket).toBeDefined();
          // eslint-disable-next-line  @typescript-eslint/no-explicit-any
          expect(f1TelemetryClient.socket.type).toBe('udp4');
        });
      });
    });
  });
  var _loop = function _loop(year) {
    var file = "src/mocks/".concat(year, ".json");
    var liner = fs.existsSync(file) ? new _nReadlines["default"](file) : null;
    var line = null;
    var lineNumber = 1;
    describe("F1 ".concat(year), function () {
      var _loop2 = function _loop2() {
        var data = JSON.parse(line.toString());
        it("L".concat(lineNumber++, ": ").concat(data.packetID), function () {
          var _data$message$data, _data$message;
          expect(true).toBeTruthy();
          var bufferData = (_data$message$data = data === null || data === void 0 ? void 0 : (_data$message = data.message) === null || _data$message === void 0 ? void 0 : _data$message.data) !== null && _data$message$data !== void 0 ? _data$message$data : data === null || data === void 0 ? void 0 : data.message;
          var parsed = parseMessage(bufferData);
          expect(parsed.m_header.m_packetFormat).toEqual(year);
          expect(bufferData.length).toEqual(_index.F1TelemetryClient.getPacketSize(data.parsed.m_header.m_packetFormat, data.parsed.m_header.m_packetId));
          expect(parsed).toEqual(data.parsed);
        });
      };
      while (liner !== null && (line = liner.next())) {
        _loop2();
      }
    });
  };
  for (var year = 2018; year <= 2023; year++) {
    _loop(year);
  }
});